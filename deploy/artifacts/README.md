# artifacts

预生成的部署包，可由 `bin/windows/push-email-server.ps1` 重新生成。

| 文件 | 说明 |
|------|------|
| `email-deploy.tar.gz` | 仅邮箱服务（解压到 `/root/email-deploy`） |
| `aws-builder-id-bundle.zip` | 无 git 时控制台安装用 |
